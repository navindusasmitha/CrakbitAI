# Crakbit CLI

Planned command-line interface for Crakbit AI security tooling.

Conceptual usage:

```bash
crak scan ./project
```

Potential output formats:

- Human-readable terminal report
- JSON
- Future SARIF-compatible export

The CLI is planned after the scanner core reaches a usable alpha state.

No production CLI has been released yet.
